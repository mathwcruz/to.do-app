import '../styles/header.scss';

export function Header() {
  return (
    <header className="header animate-ToptoBottom">
      <div>
        <img src="/logo.svg" alt="to.do"/>
      </div>
    </header>
  );
};